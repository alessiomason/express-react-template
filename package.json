{
  "name": "gestionale",
  "author": "Alessio Mason",
  "description": "A template for a web application developed using the React and Express frameworks.",
  "license": "MIT",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd server && npm start",
    "heroku-postbuild": "npm install --prefix client && npm run build --prefix client && cd server && npm install && npm run build"
  },
  "engines": {
    "node": "21.x"
  }
}
